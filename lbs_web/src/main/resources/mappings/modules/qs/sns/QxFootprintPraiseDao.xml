<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.innovate.cms.modules.qs.dao.sns.QxFootprintPraiseDao">

  	<select id="getPraise" resultType="com.innovate.cms.modules.qs.entity.sns.QxFootprintPraise">
		SELECT
		qfp.fpid,
		qfp.footid,
		qfp.uid,
		qfp.praise_uid AS 'praiseUid',
		qfp.praise_name AS 'praiseName',
		qfp.update_time AS 'updateTime',
		qfp.create_time AS 'createTime',
		qfp.del_flag AS 'delFlag'
		FROM
		qx_footprint_praise AS qfp
		WHERE
		qfp.footid = #{footid}
		AND qfp.uid = #{uid}
		AND qfp.praise_uid = #{praiseUid}
	</select>
	<insert id="savePraise">
		INSERT INTO qx_footprint_praise (
			footid,
			uid,
			praise_uid,
			praise_name,
			create_time
		)
		VALUES
			(
				#{footid},
				#{uid},
				#{praiseUid},
				#{praiseName},
				#{createTime}
			)
	</insert>
	<update id="updatePraise">
		UPDATE qx_footprint_praise AS qfp
		SET del_flag = #{delFlag}
		WHERE
			qfp.footid = #{footid}
		AND qfp.uid = #{uid}
		AND qfp.praise_uid = #{praiseUid}
	</update>
</mapper>